import { Nav } from "@/components/Nav";

export default function VenueOrders() {
  return (
    <div className="container">
      <h2>Orders</h2>
      <Nav role="v" />
      <p className="muted">Implement order queue + status updates + closure prompt.</p>
    </div>
  );
}
