import { Nav } from "@/components/Nav";

export default function VenueDashboard() {
  return (
    <div className="container">
      <h2>Venue Dashboard</h2>
      <Nav role="v" />
      <p className="muted">Implement: orders queue, accept/update status, boost hour, promo limits.</p>
    </div>
  );
}
