import { Nav } from "@/components/Nav";

export default function Buddies() {
  return (
    <div className="container">
      <h2>Buddies</h2>
      <Nav role="u" />
      <p className="muted">Buddy features are scaffolded in DB. Implement UI + endpoints next: send request, accept, ghost mode.</p>
    </div>
  );
}
